#include <stdio.h>
#include <pthread.h>
#include <semaphore.h>
#include <Windows.h>

// trying to calculate prime numbers with threads

int primes[1000];
int i, j;

void *threadFunc(void *arg)
{
	j = 4;
	int i;
	
	
	
	
	
	
	return NULL;
}



int main()
{
	
	pthread_t pth; // this is our identifier
	
	
	primes[0] = 2;
	primes[1] = 3;
	primes[2] = 5;
	int i = 3;
	
	pthread_create( &pth, NULL, threadFunc, NULL);
	int k;
	for(i = 3; i < 1000; i += 2)
	{
		while( i >= j*2 ) wait;
		
		for(k = 0; a[i] >= k * 2; k++)
		{
			
		
		
	}
	
	
	return 0;
}
